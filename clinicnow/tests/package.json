{
  "name": "clinicnow-tests",
  "private": true,
  "description": "Testes end-to-end do ClinicNow (Playwright). Rodar: npm test",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
